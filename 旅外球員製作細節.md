

# 旅外球員製作細節 / 復古票券卡交接紀錄

> 這份文件是 2026-05-13 在「旅外球員」頁面製作復古票券卡時的完整救援交接紀錄。因為前一次修改未存檔 / 被後續對話覆蓋，這裡把目前可回收的全部作法、檔案、圖片路徑、踩雷與後續重建順序集中記錄。圖片可重新製作，code 架構照這份重建。

---

## 1. 核心目標

```txt
旅外球員列表卡改成復古棒球票券 / 球員卡風格。
主畫面是 app/(tabs)/abroad.tsx。
列表卡元件是 components/vintage/VintagePlayerCard.tsx。
資料來源仍走旅外資料流，不在 UI 自己 merge live / seed。
圖片可以重製，但 code 架構與接法照這份文件重建。
```

---

## 2. 主要涉及檔案

```txt
app/(tabs)/abroad.tsx
components/vintage/VintagePlayerCard.tsx
components/AbroadPlayerAvatar.tsx
data/abroadPlayers.ts
server/data/abroadPlayers.live.json
hooks/useAbroadLiveData.ts
lib/viewModels/abroadPlayerViewModel.ts
constants/teamLogos.ts
assets/abroad
assets/yaren_one_icons_png_pack
```

目前主要應修改：

```txt
components/vintage/VintagePlayerCard.tsx
data/abroadPlayers.ts
```

暫時不要再重寫：

```txt
components/AbroadPlayerAvatar.tsx
```

原因：最後方向是讓 `VintagePlayerCard` 優先用本機圖片 map 顯示球員圖，而不是一直改 `AbroadPlayerAvatar` 內部遮罩。

---

## 3. abroad.tsx 原本接法

`app/(tabs)/abroad.tsx` 只負責把 player item 傳進卡片：

```tsx
<VintagePlayerCard
  key={item.id}
  player={item}
  favorite={favorite}
  onPress={() => router.push(`/abroad/${item.id}`)}
  onToggleFavorite={() => toggleAbroadFavorite(item.id)}
/>
```

重點：

```txt
abroad.tsx 不應該接本機圖片。
abroad.tsx 不應該處理圖層。
abroad.tsx 不應該判斷 officialPhotoUrl / local image。
VintagePlayerCard 自己決定卡片 UI 與圖片顯示。
```

---

## 4. VintagePlayerCard 最終方向

```txt
1. 使用 小長格.png 當整張卡片底圖。
2. 球員本機圖優先於 officialPhotoUrl。
3. officialPhotoUrl 有值時可當遠端圖 fallback。
4. 沒本機圖也沒 officialPhotoUrl 時才 fallback 回 AbroadPlayerAvatar。
5. 右側顯示大背號，顯示前移除 # / ＃。
6. 名字下方不顯示背號。
7. 守備位置不可重複。
8. 中文使用 CN_FONT，英文 / 數字使用 APP_FONT。
9. 所屬隊伍 logo 顯示在名字右側附近，但不要塞進 title row。
10. 聯盟 stamp 依 MLB / NPB / KBO 自動切換。
11. 隊伍 logo 最後決定維持彩色，不要 tintColor 黑白。
```

---

## 5. VintagePlayerCard 建議 import

```tsx
import { Image, StyleSheet, Text, TouchableOpacity, View, type ImageSourcePropType } from 'react-native';
import { Ionicons } from '@expo/vector-icons';

import AbroadPlayerAvatar from '../AbroadPlayerAvatar';
import { getTeamLogoSource } from '../../constants/teamLogos';
import {
  formatAbroadLevelLine,
  formatAbroadTeamLine,
  type AbroadPlayerLike,
} from '../../lib/viewModels/abroadPlayerViewModel';
```

注意：

```txt
ImageSourcePropType 是為了 localAbroadPhotos map 型別。
getTeamLogoSource 不吃 string。
getTeamLogoSource 目前可用寫法是 getTeamLogoSource({ logoKey })。
name / abbreviation / code 都曾經報 TS 錯，避免再傳。
```

---

## 6. 字體設定與規則

```ts
const APP_FONT = 'CityBurn';
const CN_FONT = 'ZaoZiGongFangXingHei';
```

規則：

```txt
英文 / 數字：APP_FONT。
中文：CN_FONT。
同一行混中英文：拆成多個 nested Text 分別套 style。
不要整行都改 CN_FONT，否則英文會跑掉。
不要整行都改 APP_FONT，否則中文會跑掉。
```

混合文字工具函式曾使用：

```tsx
function splitVintageMixedText(text: string) {
  return text.match(/[\u4e00-\u9fff\u3400-\u4dbf\uf900-\ufaff]+|[^\u4e00-\u9fff\u3400-\u4dbf\uf900-\ufaff]+/g) ?? [];
}

function isChineseText(text: string) {
  return /[\u4e00-\u9fff\u3400-\u4dbf\uf900-\ufaff]/.test(text);
}

function renderVintageMixedText(text: string, chineseStyle: object, englishStyle: object) {
  return splitVintageMixedText(text).map((part, index) => (
    <Text key={`${part}-${index}`} style={isChineseText(part) ? chineseStyle : englishStyle}>
      {part}
    </Text>
  ));
}
```

---

## 7. 圖層規則

曾用過的圖層邏輯：

```ts
const VINTAGE_LAYER = {
  texture: 1,
  portrait: 2,
  ticket: 4,
  title: 8,
  info: 9,
  number: 10,
  stamp: 12,
};
```

意思：

```txt
數字越大越上層。
portrait = 球員頭像。
ticket = 小長格.png 底圖 / 前景票券框。
title = 名字 / 文字。
info = stamp / 資訊圖章。
number = 備用數字層。
stamp = 右側背號。
texture = 紙張材質層。
```

關鍵踩雷：

```txt
如果想讓小長格.png 壓在頭像前面：ticket 必須大於 portrait。
如果頭像要露出框線外：外層 card / avatarSlot 不能 overflow hidden。
如果看到小長格.png 被擋，先查 texture / overlay / zIndex，不要先改圖片。
```

---

## 8. Layout token 建議

曾用過的基準值：

```ts
const VINTAGE_LAYOUT = {
  cardAspectRatio: 3.8,
  radius: 0,
  photoLeft: 12,
  photoTop: 14,
  photoSize: 90,
  stampLeft: 100,
  stampTop: 15,
  stampSize: 32,
  contentLeft: 124,
  contentTop: 15,
  contentWidth: 250,
  statusRight: 3,
  statusTop: 10,
  statusWidth: 88,
  statusHeight: 122,
  ticketScale: 1.02,
};
```

微調對照：

```txt
photoLeft：頭像往左 / 往右。
photoTop：頭像往上 / 往下。
photoSize：頭像大小。
stampLeft：聯盟 stamp 左右位置。
stampTop：聯盟 stamp 上下位置。
stampSize：聯盟 stamp 大小。
contentLeft：名字與下方資訊左右位置。
contentTop：名字與下方資訊上下位置。
contentWidth：文字區寬度。
ticketScale：小長格.png 縮放。
statusRight：右側背號左右位置。
statusTop：右側背號上下位置。
statusWidth / statusHeight：右側背號區域。
```

---

## 9. 透明度 token

曾新增集中控制：

```ts
const VINTAGE_OPACITY = {
  text: 0.86,
  teamLogo: 0.78,
  leagueStamp: 0.72,
  number: 0.86,
};
```

用途：

```txt
text：名字與資訊文字透明度。
teamLogo：隊伍 logo 透明度。
leagueStamp：MLB / NPB / KBO stamp 透明度。
number：右側大背號透明度。
```

注意：

```txt
隊伍 logo 曾經加過 tintColor: '#111111' 變黑白。
後來已決定改回彩色。
所以 teamLogoStamp 不要保留 tintColor。
```

---

## 10. 圖片資源路徑

目前實際路徑：

```txt
assets/yaren_one_icons_png_pack/小長格.png
assets/yaren_one_icons_png_pack/MLBstamp.png
assets/yaren_one_icons_png_pack/NPBstamp.png
assets/yaren_one_icons_png_pack/KBOstamp.png
```

曾誤判但不存在的資料夾：

```txt
assets/MLBstamp
assets/NPBstamp
assets/KBOstamp
```

球員本機圖路徑：

```txt
assets/abroad
```

當時列出的本機球員圖：

```txt
assets/abroad/an-ko-lin.png
assets/abroad/chia-cheng-lin.png
assets/abroad/chia-hao-song.png
assets/abroad/chun-wei-chang.png
assets/abroad/huang-chung-hsiang.png
assets/abroad/jo-hsi-hsu.png
assets/abroad/ruei-yang-gu-lin.png
assets/abroad/tzu-chen-sha.png
assets/abroad/yen-cheng-wang.png
assets/abroad/yi-lei-sun.png
assets/abroad/Shosei徐翔聖.png
```

圖片重製後，只要檔名不同，更新 `localAbroadPhotos` map 即可。

---

## 11. vintageImages 建議寫法

```ts
const vintageImages = {
  playerTicketBg: require('../../assets/yaren_one_icons_png_pack/小長格.png'),
  mlbStamp: require('../../assets/yaren_one_icons_png_pack/MLBstamp.png'),
  npbStamp: require('../../assets/yaren_one_icons_png_pack/NPBstamp.png'),
  kboStamp: require('../../assets/yaren_one_icons_png_pack/KBOstamp.png'),
};
```

注意：

```txt
React Native 本機圖片不能用 JSON 字串路徑。
必須在 TS / TSX 用 require()。
不能在 server/data/abroadPlayers.live.json 寫圖片路徑期待 Image 直接吃。
```

---

## 12. localAbroadPhotos map 作法

暫時做法放在 `VintagePlayerCard.tsx`：

```ts
const localAbroadPhotos: Record<string, ImageSourcePropType> = {
  'an-ko-lin': require('../../assets/abroad/an-ko-lin.png'),
  'chia-cheng-lin': require('../../assets/abroad/chia-cheng-lin.png'),
  'chia-hao-song': require('../../assets/abroad/chia-hao-song.png'),
  'chun-wei-chang': require('../../assets/abroad/chun-wei-chang.png'),
  'huang-chung-hsiang': require('../../assets/abroad/huang-chung-hsiang.png'),
  'jo-hsi-hsu': require('../../assets/abroad/jo-hsi-hsu.png'),
  'shosei-hsu': require('../../assets/abroad/Shosei徐翔聖.png'),
  'hsiang-sheng-hsu': require('../../assets/abroad/Shosei徐翔聖.png'),
  'ruei-yang-gu-lin': require('../../assets/abroad/ruei-yang-gu-lin.png'),
  'tzu-chen-sha': require('../../assets/abroad/tzu-chen-sha.png'),
  'yen-cheng-wang': require('../../assets/abroad/yen-cheng-wang.png'),
  'yi-lei-sun': require('../../assets/abroad/yi-lei-sun.png'),
};
```

重要：

```txt
key 必須等於 player.id。
如果圖片沒出現，先查 player.id，不要猜檔名。
徐翔聖可能 id 不是 shosei-hsu / hsiang-sheng-hsu，要用 grep 查 data/abroadPlayers.ts。
```

查 id 指令：

```bash
grep -n -A20 -B5 "徐翔聖\|Shosei" data/abroadPlayers.ts
grep -n -A20 -B5 "徐翔聖\|Shosei" server/data/abroadPlayers.live.json
```

長期建議：

```txt
localAbroadPhotos 不要長期留在 UI component。
應移到 constants/abroadPlayerImages.ts 或 data/manual/abroadPlayerManualPatches.ts。
```

---

## 13. 本機圖 / 遠端圖顯示邏輯

建議在 `VintagePlayerCard` 內：

```ts
const localPhotoSource = localAbroadPhotos[player.id];
const remotePhotoSource = player.officialPhotoUrl ? { uri: player.officialPhotoUrl } : undefined;
const portraitSource = localPhotoSource ?? remotePhotoSource;
```

JSX：

```tsx
<View style={styles.avatarSlot}>
  {portraitSource ? (
    <Image
      source={portraitSource}
      style={styles.portraitImage}
      resizeMode="contain"
    />
  ) : (
    <AbroadPlayerAvatar
      name={player.name}
      team={player.team}
      league={player.league}
      level={player.level}
      teamCode={player.teamMeta?.code ?? player.teamMeta?.abbreviation}
      logoKey={player.teamMeta?.logoKey}
      photoUri={player.officialPhotoUrl}
      teamColor={player.teamColor}
      size={VINTAGE_LAYOUT.photoSize}
      textSize={24}
      borderRadius={0}
    />
  )}
</View>
```

注意：

```txt
曾加過 noMask，但 AbroadPlayerAvatar Props 沒有 noMask，會 TS2322。
所以不要傳 noMask，除非先正式修改 AbroadPlayerAvatar Props。
目前用 Image 直接顯示本機圖，是避免本機圖被 AbroadPlayerAvatar 內部裁切。
```

---

## 14. 聯盟 stamp 切換

建議函式：

```ts
function getVintageLeagueStampSource(player: AbroadPlayerLike) {
  const leagueText = `${player.league ?? ''} ${player.level ?? ''}`.toLowerCase();

  if (leagueText.includes('npb') || leagueText.includes('日職')) {
    return vintageImages.npbStamp;
  }

  if (leagueText.includes('kbo') || leagueText.includes('韓職')) {
    return vintageImages.kboStamp;
  }

  return vintageImages.mlbStamp;
}
```

在 component 內：

```ts
const leagueStampSource = getVintageLeagueStampSource(player);
```

JSX：

```tsx
<Image
  source={leagueStampSource}
  style={styles.passportStamp}
  resizeMode="contain"
/>
```

style：

```ts
passportStamp: {
  position: 'absolute',
  left: VINTAGE_LAYOUT.stampLeft,
  top: VINTAGE_LAYOUT.stampTop,
  width: VINTAGE_LAYOUT.stampSize,
  height: VINTAGE_LAYOUT.stampSize,
  opacity: VINTAGE_OPACITY.leagueStamp,
  zIndex: VINTAGE_LAYER.info,
  transform: [{ rotate: '-9deg' }],
},
```

---

## 15. 球隊 logo 顯示

正確來源：

```ts
const teamLogoSource = getTeamLogoSource({
  logoKey: player.teamMeta?.logoKey,
});
```

不要這樣寫，會報錯或型別不合：

```ts
getTeamLogoSource(player.teamMeta?.logoKey ?? player.team)
getTeamLogoSource({ name: player.team })
getTeamLogoSource({ abbreviation: player.teamMeta?.abbreviation })
getTeamLogoSource({ code: player.teamMeta?.code })
```

JSX：

```tsx
{teamLogoSource ? (
  <Image
    source={teamLogoSource}
    style={styles.teamLogoStamp}
    resizeMode="contain"
  />
) : null}
```

style：

```ts
teamLogoStamp: {
  position: 'absolute',
  left: VINTAGE_LAYOUT.contentLeft + 145,
  top: VINTAGE_LAYOUT.contentTop - 2,
  width: 34,
  height: 34,
  opacity: VINTAGE_OPACITY.teamLogo,
  zIndex: VINTAGE_LAYER.title,
},
```

注意：

```txt
球隊 logo 曾改成黑白：tintColor: '#111111'
後來決定改回彩色，請刪掉 tintColor。
```

---

## 16. 名字 / 下方資訊 / 背號顯示

名字：

```tsx
<Text style={styles.playerName} numberOfLines={1}>{player.name}</Text>
```

名字下方第一行：

```tsx
<Text style={styles.playerMeta} numberOfLines={1}>
  {renderVintageMixedText(formatAbroadTeamLine(player), styles.playerMetaChinese, styles.playerMetaEnglish)}
</Text>
```

名字下方第二行：

```tsx
<Text style={styles.playerLine} numberOfLines={1}>
  {renderVintageMixedText(buildVintagePlayerSubLine(player), styles.playerLineChinese, styles.playerLineEnglish)}
</Text>
```

右側背號：

```tsx
<Text style={styles.statusNumber}>{String(player.number ?? '').replace(/[#＃]/g, '')}</Text>
```

規則：

```txt
背號只在右側大數字出現。
名字下方不要重複顯示背號。
右側背號顯示前移除 # / ＃。
沒有背號則空白。
```

---

## 17. 守備位置去重

normalize 函式：

```ts
function normalizeVintageInfoText(value?: string | null) {
  const normalized = String(value ?? '')
    .replace(/[#＃]/g, '')
    .replace(/[\s・／/|｜,，]+/g, '')
    .trim()
    .toLowerCase();

  const positionAliases: Record<string, string> = {
    p: '投手',
    pitcher: '投手',
    投手: '投手',
    sp: '投手',
    rp: '投手',
    c: '捕手',
    catcher: '捕手',
    捕手: '捕手',
    '1b': '一壘手',
    一壘手: '一壘手',
    '2b': '二壘手',
    二壘手: '二壘手',
    '3b': '三壘手',
    三壘手: '三壘手',
    ss: '游擊手',
    游擊手: '游擊手',
    if: '內野手',
    內野手: '內野手',
    of: '外野手',
    外野手: '外野手',
    lf: '外野手',
    cf: '外野手',
    rf: '外野手',
    dh: '指定打擊',
    指定打擊: '指定打擊',
  };

  return positionAliases[normalized] ?? normalized;
}
```

build function：

```ts
function buildVintagePlayerSubLine(player: AbroadPlayerLike) {
  const levelLine = formatAbroadLevelLine(player);
  const positionText = player.position ?? '';
  const parts = levelLine
    .split(/[・／/|｜,，()（）\s]+/)
    .map((item) => item.trim())
    .filter(Boolean);
  const normalizedParts = new Set(parts.map(normalizeVintageInfoText));
  const positionParts = positionText
    .split(/[・／/|｜,，()（）\s]+/)
    .map((item) => item.trim())
    .filter(Boolean);

  positionParts.forEach((positionPart) => {
    const normalizedPosition = normalizeVintageInfoText(positionPart);

    if (normalizedPosition && !normalizedParts.has(normalizedPosition)) {
      parts.push(positionPart);
      normalizedParts.add(normalizedPosition);
    }
  });

  return parts.join('');
}
```

注意：

```txt
李灝宇曾出現兩次 3B。
原因是 formatAbroadLevelLine(player) 可能已含 3B，而 player.position 也是 3B。
所以 position 需要拆 token 後 normalize 比對。
最後 return parts.join('') 是因為使用者要求把空格 / 分隔符刪除。
```

---

## 18. 主要 style 片段

```ts
card: {
  width: '100%',
  aspectRatio: VINTAGE_LAYOUT.cardAspectRatio,
  marginBottom: VINTAGE_SPACE.cardGap,
  borderRadius: VINTAGE_LAYOUT.radius,
  overflow: 'visible',
  shadowColor: VINTAGE_COLOR.shadow,
  shadowOpacity: 0.16,
  shadowRadius: VINTAGE_SPACE.shadowRadius,
  shadowOffset: { width: 0, height: VINTAGE_SPACE.shadowY },
  elevation: 2,
},
cardTicketBg: {
  ...StyleSheet.absoluteFillObject,
  overflow: 'visible',
},
ticketOverlay: {
  ...StyleSheet.absoluteFillObject,
  width: '100%',
  height: '100%',
  borderRadius: VINTAGE_LAYOUT.radius,
  opacity: 1,
  zIndex: VINTAGE_LAYER.ticket,
  transform: [{ scale: VINTAGE_LAYOUT.ticketScale }],
},
avatarSlot: {
  position: 'absolute',
  left: VINTAGE_LAYOUT.photoLeft,
  top: VINTAGE_LAYOUT.photoTop,
  width: VINTAGE_LAYOUT.photoSize,
  height: VINTAGE_LAYOUT.photoSize,
  overflow: 'visible',
  alignItems: 'center',
  justifyContent: 'center',
  zIndex: VINTAGE_LAYER.portrait,
  transform: [{ rotate: '-2deg' }],
},
portraitImage: {
  width: VINTAGE_LAYOUT.photoSize,
  height: VINTAGE_LAYOUT.photoSize,
},
```

文字 style 概念：

```ts
playerName: {
  color: '#111111',
  fontFamily: CN_FONT,
  fontSize: 26,
  fontWeight: '900',
  lineHeight: 30,
  letterSpacing: 0.3,
  flexShrink: 1,
  opacity: VINTAGE_OPACITY.text,
},
playerMeta: {
  color: VINTAGE_COLOR.ink,
  fontSize: 12,
  fontWeight: '900',
  lineHeight: 14,
  letterSpacing: 0.7,
  marginTop: 4,
},
playerMetaChinese: {
  color: VINTAGE_COLOR.ink,
  fontFamily: CN_FONT,
  fontSize: 12,
  fontWeight: '900',
  lineHeight: 14,
  letterSpacing: 0.7,
  opacity: VINTAGE_OPACITY.text,
},
playerMetaEnglish: {
  color: VINTAGE_COLOR.ink,
  fontFamily: APP_FONT,
  fontSize: 12,
  fontWeight: '900',
  lineHeight: 14,
  letterSpacing: 0.7,
  opacity: VINTAGE_OPACITY.text,
},
playerLine: {
  color: '#111111',
  fontSize: 11,
  fontWeight: '900',
  lineHeight: 14,
  marginTop: 5,
},
playerLineChinese: {
  color: '#111111',
  fontFamily: CN_FONT,
  fontSize: 11,
  fontWeight: '900',
  lineHeight: 14,
  opacity: VINTAGE_OPACITY.text,
},
playerLineEnglish: {
  color: '#111111',
  fontFamily: APP_FONT,
  fontSize: 11,
  fontWeight: '900',
  lineHeight: 14,
  opacity: VINTAGE_OPACITY.text,
},
```

---

## 19. 背號資料修正紀錄

林安可資料曾顯示錯誤：

```ts
number: '77'
```

查證後應修正為：

```ts
number: '73'
```

原因：

```txt
林安可在西武獅背號為 73，不是 77。
77 是他在統一時期熟悉的背號。
西武官方 / NPB 官方資料顯示背號 73。
```

修正位置：

```txt
data/abroadPlayers.ts
```

如果畫面仍顯示舊背號，查：

```txt
server/data/abroadPlayers.live.json
```

因為 live json 可能覆蓋 seed。

查所有背號指令：

```bash
grep -R "\"number\"\|number:" -n data server/data | grep -i "an-ko-lin\|林安可\|number"
grep -R "id:\|name:\|number:" -n data/abroadPlayers.ts
grep -R "\"id\"\|\"name\"\|\"number\"" -n server/data/abroadPlayers*.json
```

後續要做：

```txt
系統性查證所有旅外球員背號，尤其 NPB / KBO / MiLB。
```

---

## 20. 常見錯誤與踩雷

```txt
1. 不要直接重寫 AbroadPlayerAvatar；目前旅外票券卡用 localAbroadPhotos 優先顯示本機圖。
2. React Native 本機圖片不能從 JSON 字串路徑直接 require，必須在 TS 檔用 require map。
3. assets/MLBstamp / assets/NPBstamp / assets/KBOstamp 資料夾不存在；stamp 圖實際在 assets/yaren_one_icons_png_pack/。
4. getTeamLogoSource 不是吃 string，也不是吃 name / abbreviation / code；目前可用的是 getTeamLogoSource({ logoKey })。
5. 隊伍 logo 若要黑白可加 tintColor，但目前已改回彩色，不要再保留 tintColor。
6. 小長格.png 不要加紙色底，不要用 stretch 硬拉；目前靠 ticketScale / contain 控制。
7. 若頭像被切，先檢查 card / avatarSlot / Image resizeMode，不要直接懷疑資料。
8. 修改 code 前仍必須先告知要改哪個檔案。
9. 若 tsc 顯示舊錯誤但 Xcode 看似已改，先存檔再 grep。
10. 若圖片沒出現，先查檔名大小寫與 player.id。
```

---

## 21. 重建順序建議

```txt
1. 先確認 abroad.tsx 仍是只 render VintagePlayerCard。
2. 重建 components/vintage/VintagePlayerCard.tsx 的 import、token、image map。
3. 接回 小長格.png。
4. 接回 localAbroadPhotos。
5. 接回 player.officialPhotoUrl fallback。
6. 接回 league stamp。
7. 接回 teamLogoSource({ logoKey })。
8. 接回名字 / meta / line / 背號排版。
9. 接回中英文混合字體 renderVintageMixedText。
10. 接回守備位置去重 buildVintagePlayerSubLine。
11. 修正林安可背號 73。
12. 跑 npx tsc --noEmit。
13. 若畫面資料不對，查 server/data/abroadPlayers.live.json 是否覆蓋。
```

---

## 22. 後續架構建議

```txt
1. 建議建立 data/manual/abroadPlayerManualPatches.ts。
2. 把背號、圖片、teamMeta、manual 修正集中，不要散在 UI component。
3. 後續 localAbroadPhotos 不宜長期放在 VintagePlayerCard；應移到 constants/ 或 data/manual/。
4. 若 live json 會覆蓋 seed number，需改 mergeAbroadPlayerViewModels：manual number 優先於 remote number。
5. 建議建立一個 verify:abroad-numbers 腳本，定期列出 seed / live / manual 背號差異。
```

---

## 23. 每次修改前規則

```txt
在此 APP 專案中，每次改 code 前，必須先告知需要改動哪一個檔案。
不要直接改 code。
使用者明確要求「改」後，才使用 patch。
```
